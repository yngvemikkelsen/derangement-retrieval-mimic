# Core analysis. No scikit-learn: modelling is statsmodels throughout so that
# cluster-robust covariance and Wald contrasts are explicit.
numpy>=1.24
pandas>=2.0
scipy>=1.10
statsmodels>=0.14
pyarrow>=12.0          # parquet
rank-bm25>=0.2.2       # lexical baseline

# Stage 2 only: encoding. Not needed for stage 3, which reads the cache.
torch>=2.0
transformers>=4.35

# Optional
matplotlib>=3.7        # figures
